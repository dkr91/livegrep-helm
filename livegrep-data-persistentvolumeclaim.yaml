apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: livegrep-data
  name: livegrep-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
