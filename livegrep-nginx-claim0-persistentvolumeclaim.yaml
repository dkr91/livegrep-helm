apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: livegrep-nginx-claim0
  name: livegrep-nginx-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
