apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: livegrep-frontend-claim0
  name: livegrep-frontend-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
